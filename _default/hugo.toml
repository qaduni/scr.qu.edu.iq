baseURL = 'https://scr.qu.edu.iq/'
locale = 'ar'
defaultContentLanguage = 'ar'
title = "قسم البعثات والعلاقات الثقافية - جامعة القادسية"
buildFuture = true
theme = "qu.theme"

[module]
  [[module.mounts]]
    source = "assets"
    target = "assets"
  
  [[module.mounts]]
    source = "static/media"
    target = "assets/media"

[pagination]
  pagerSize = 10

[languages]
  [languages.ar]
    label = "العربية"
    weight = 1
    direction = "rtl"
    title = "قسم البعثات والعلاقات الثقافية"
    locale = "ar"

    [languages.ar.params]
      description = "قسم البعثات والعلاقات الثقافية في جامعة القادسية"
      shortTitle  = "قسم ضمان الجودة"
      parentName  = "جامعة القادسية"
      parentURL   = "https://qu.edu.iq"

      [languages.ar.params.contact]
        address = "جامعة القادسية، الديوانية، محافظة القادسية، العراق"

  [languages.en]
    label = "English"
    weight = 2
    direction = "ltr"
    title = "Scholarship & Culture Relationship Department"
    locale = "en"

    [languages.en.params]
      description = "Scholarship & Culture Relationship Department at Al-Qadisiyah University"
      shortTitle  = "Quality Assurance Department"
      parentName  = "Al-Qadisiyah University"
      parentURL   = "https://qu.edu.iq"

      [languages.en.params.contact]
        address = "Al-Qadisiyah University, Al-Diwaniyah, Al-Qadisiyah Province, Iraq"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      # Strapi authors are trusted, but keep this 'false' so a compromised CMS
      # account can't inject raw <script>/<iframe>/on-event handlers via
      # markdown bodies.
      unsafe = false

[outputs]
  home = ["HTML", "RSS", "sitemap", "WebAppManifest"]
  section = ["HTML", "RSS"]
  page = ["HTML"]

[sitemap]
  changefreq = "weekly"
  filename = "sitemap.xml"
  priority = 0.5
