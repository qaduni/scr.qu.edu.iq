---
title: "Contact"
description: "Get in touch"
---

Drop the `{{</* contact-form */>}}` shortcode here once you set
`params.contactFormEndpoint` in `hugo.toml` — until then the shortcode
renders a friendly mailto fallback that uses `params.contact.email`.

{{< contact-form >}}
